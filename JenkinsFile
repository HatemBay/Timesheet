pipeline {
    agent any
    stages {
        stage('git repo & clean') {
            steps {
            //    ./ "rmdir  /s /q TicketBookingServiceJunitTesting"
                ./ "git clone https://github.com/HatemBay/Timesheet.git"
                ./ "mvn clean -f project_pipeline"
            }
        }
        stage('install') {
            steps {
                ./ """mvn clean install"""
            }
        }
        stage('test') {
            steps {
                bat"""mvn test"""
            }
        }
        stage('package') {
            steps {
                bat "mvn package -f project_pipeline"
            }
        }
    }
}
