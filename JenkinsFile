pipeline {
    agent any
    stages {
        stage('git repo & clean') {
            steps {
            //    bat "rmdir  /s /q TicketBookingServiceJunitTesting"
                bat "git clone https://github.com/HatemBay/Timesheet.git"
                bat "mvn clean -f project_pipeline"
            }
        }
        stage('install') {
            steps {
                ./"""mvn clean install"""
            }
        }
        stage('test') {
            steps {
                bat"""mvn test"""
            }
        }
        stage('package') {
            steps {
                bat "mvn package -f project_pipeline"
            }
        }
    }
}
